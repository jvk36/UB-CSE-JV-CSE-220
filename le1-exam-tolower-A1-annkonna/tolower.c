#include <stdbool.h>

/* Returns true if the character c is an uppercase ASCII character, and
 * false otherwise. */
bool cse_isupper(char c) {
    return false;
}

/* Transforms every uppercase ASCII character in str, which is a
 * standard C string, to its lowercase ASCII equivalent. */
void cse_strlower(char str[]) {
}
